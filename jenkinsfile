pipeline {
    agent any

    stages{
        stage ('test') {
            steps {
                echo "Starting Unit tests"
                sh """
                    pwd
                    ls -l
                """
            }

        }

    }
}
